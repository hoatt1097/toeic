<%= form_for(operator_user) do |f| %>
  <% if operator_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(operator_user.errors.count, "error") %> prohibited this operator_user from being saved:</h2>

      <ul>
      <% operator_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
