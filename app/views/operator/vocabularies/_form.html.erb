<%= form_for(operator_vocabulary) do |f| %>
  <% if operator_vocabulary.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(operator_vocabulary.errors.count, "error") %> prohibited this operator_vocabulary from being saved:</h2>

      <ul>
      <% operator_vocabulary.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
