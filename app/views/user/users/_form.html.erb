<%= form_for(user_user) do |f| %>
  <% if user_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_user.errors.count, "error") %> prohibited this user_user from being saved:</h2>

      <ul>
      <% user_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
